{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. 
  
  TODO: Lock this down! Full access is just for testing and getting started.

  Rules to add:
  rooms/${id}/messages/${message} is only writable if its uid === auth.uid
  */
  "rules": {
    ".read": true,
    ".write": true
  }
}